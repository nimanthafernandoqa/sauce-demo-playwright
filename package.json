{
  "name": "sauce-demo-playwright",
  "version": "1.0.0",
  "description": "Playwright TS test suite for Sauce Demo with AI agents",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "generate-tests": "ts-node agents/test-generator.ts",
    "heal-tests": "ts-node agents/self-healer.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0"
  }
}
